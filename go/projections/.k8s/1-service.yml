apiVersion: v1
kind: Service
metadata:
  name: projections-go
  namespace: fraym
spec:
  type: ClusterIP
  selector:
    app: projections-go
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
