apiVersion: v1
kind: Service
metadata:
  name: streams-go
  namespace: fraym
spec:
  type: ClusterIP
  selector:
    app: streams-go
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
